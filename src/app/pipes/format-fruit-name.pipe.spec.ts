import { FormatFruitNamePipe } from './format-fruit-name.pipe';

describe('FormatFruitNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFruitNamePipe();
    expect(pipe).toBeTruthy();
  });
});
